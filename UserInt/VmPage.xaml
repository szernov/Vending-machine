<Page x:Class="UserInt.VmPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UserInt"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="400"
      Title="VmPage">

    <Grid>
        <ListView x:Name="VmListVIew" HorizontalAlignment="Left" Height="300" VerticalAlignment="Top" Width="400" Loaded="VmListVIew_Loaded">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Id" DisplayMemberBinding="{Binding Path=Id}" Width="100" />
                    <GridViewColumn Header="Локация" DisplayMemberBinding="{Binding Path=Location}" Width="100"/>
                    <GridViewColumn Header="Банкноты" DisplayMemberBinding="{Binding Path=Banknotes}" Width="100"/>
                    <GridViewColumn Header="Монеты" DisplayMemberBinding="{Binding Path=Coins}" Width="100" />
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
